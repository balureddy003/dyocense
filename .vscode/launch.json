{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run kernel.pipeline (module)",
      "type": "python",
      "request": "launch",
      "module": "kernel.pipeline",
      "cwd": "${workspaceFolder}",
      "justMyCode": true,
      "console": "integratedTerminal",
      "env": {
        "PYTHONPATH": "${workspaceFolder}"
      }
    }
  ]
}