# Schema Location

Schema sources now live under `packages/contracts/schemas/`. This directory is retained for backwards compatibility with earlier documentation and will eventually hold specification walkthroughs that reference the canonical schemas.
